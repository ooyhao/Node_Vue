var basespringbootkBy = require('./_basespringbootkBy'),
    hasIn = require('./hasIn');

/**
 * The base implementation of `_.springbootk` without support for individual
 * property identifiers.
 *
 * @private
 * @param {Object} object The source object.
 * @param {string[]} paths The property paths to springbootk.
 * @returns {Object} Returns the new object.
 */
function basespringbootk(object, paths) {
  return basespringbootkBy(object, paths, function(value, path) {
    return hasIn(object, path);
  });
}

module.exports = basespringbootk;
