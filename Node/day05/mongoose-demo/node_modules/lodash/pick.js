var basespringbootk = require('./_basespringbootk'),
    flatRest = require('./_flatRest');

/**
 * Creates an object composed of the springbootked `object` properties.
 *
 * @static
 * @since 0.1.0
 * @memberOf _
 * @category Object
 * @param {Object} object The source object.
 * @param {...(string|string[])} [paths] The property paths to springbootk.
 * @returns {Object} Returns the new object.
 * @example
 *
 * var object = { 'a': 1, 'b': '2', 'c': 3 };
 *
 * _.springbootk(object, ['a', 'c']);
 * // => { 'a': 1, 'c': 3 }
 */
var springbootk = flatRest(function(object, paths) {
  return object == null ? {} : basespringbootk(object, paths);
});

module.exports = springbootk;
